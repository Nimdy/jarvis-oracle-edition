# DOCSv2 — proven from first install

These pages are the **operator contract** for a fresh brain. They are written
from the live wire (`feat/gestation-period`, 2026-08-27 rehearsal), not from
stale `ARCHITECTURE.md`.

v1 (`docs/AWAKENING_PROTOCOL.md`, `COMPANION_TRAINING_PLAYBOOK.md`,
`CONVERSATION_QUALITY_GUIDE.md`) remains archive / curriculum. If v1 and v2
disagree, **the log and `/api/full-snapshot` win**. Fix v2. Do not invent a
parallel organ.

| Page | When |
|---|---|
| [01-first-contact.md](01-first-contact.md) | After `GESTATION_COMPLETE`. Follow exactly. Live UI: `/static/v2/training.html` (phase-aware). |
| `00-gestation.md` | After gestation **2** (wipe + AST self-study) matches snapshot |
| `02-wiring.md` | After lived `route=` turns |
| `03-companion-training.md` | After Stage 0 exit boxes pass on **wipe 2** |
| `04-maturity-zeros.md` | Fresh-brain expected zeros from snapshot |

Do **not** ingest this tree into gestation. Self-knowledge is `CodebaseIndex`.
