# TODO v2: Post-Release Engineering Plan

**Created:** 2026-04-24
**Status:** Active. Successor to `TODO.md` for net-new engineering work after the
2026-05-10 open-source release pass landed (P2.7).
**Posture:** Continuity-preserving. Surgical. Architecture-first.

`TODO.md` remains the historical operator runbook + Phase D rollout checklist +
gated future-feature notes. `TODO_V2.md` is the focused, lane-based plan for
the engineering items that actually move the system forward, sequenced and
scoped so a fresh agent can pick any one lane and ship it without re-deriving
context.

---

## 0. Mission

The release is done. The five truth-audit lanes (P1.1 to P1.10), the continuity
pivot, and the seven Phase 2 lanes (P2.1 to P2.7) all landed. `current_ok`,
`prior_attested_ok`, `activation_ok`, the audit ledger, the truth-boundary
counters, and the schema audits are all green where they need to be and
honestly red where they need to be.

The work in this file is post-release engineering inside the same architecture.
Every lane below is additive, not corrective. Nothing in this file may break
or relax a load-bearing invariant. If a lane needs to widen an invariant to
ship, the lane stops and the invariant is debated separately.

---

## 1. Hard constraints (the contract every lane must honor)

These are non-negotiable. A lane that requires breaking one of these is
out of scope.

| Constraint | Source | What "honoring it" means in practice |
| --- | --- | --- |
| Continuity-preserving (no brain wipe) | Operator directive 2026-04-23 | The live brain on `192.168.1.222` keeps running. No `reset-brain.sh`. The 46 GiB tarball at `~/jarvis_continuity_backup_2026-04-23.tar.gz` (sha256 `b9e92bf6...`) is the safety net, never the workflow. |
| Local commits only, no GitHub push | Operator directive | Every lane lands as a local commit on `main`. `sync-desktop.sh` is the only path to remote testing. No `git push` of any kind. No conversational chatter in commit messages. |
| Tri-layer cognition separation | Architecture | Symbolic truth layer is canonical. Neural intuition layer is shadow-then-promote. LLM is bootstrap teacher and articulation surface only. No lane may put the LLM into canonical state, autonomy decisions, or governance verdicts. |
| Three-axis Phase 6.5 invariants | `tests/test_l3_*.py`, `tests/test_autonomy_*.py` | `current_ok` stays live-sourced. `prior_attested_ok` stays hash-verified. `activation_ok` stays distinct. No lane may collapse two of these into one through any code path, including indirect ones (snapshot caches, restart hydration, ledger replay). |
| Truth boundary on synthetic perception | P2.2 evidence | Synthetic exercise routes never write persistent memory, identity, conversation, or TTS state. The five leak counters stay at zero. |
| Append-only audit ledger | `AutonomyAuditLedger` | No lane edits a past entry. Rotation is the only legal mutation. |
| NN Maturity Flywheel (AGENTS.md Principle #2) | AGENTS.md | New NN specialists ship shadow-only first. Promotion is gated by accuracy and dwell. No "ship it primary" for any new specialist, ever. |
| No auto-promotion of autonomy | `test_l3_promotion_invariant.py` | L3 activation requires explicit operator action. No lane introduces a code path that promotes without `Approve`. |
| Schema audit zero violations | `brain/scripts/schema_emission_audit.py` | If a new declared surface is added, either a writer ships in the same commit or the surface is whitelisted in `FUTURE_ONLY_*` with a precise reason string. |
| Regression baseline | P2.7 launch artifact | 4158 passed / 4 pre-existing flakes / 5 skipped / 3 deselected. New lanes must hold this baseline or improve it. Net new failures block the lane. |
| Status markers stay honest | `/api/meta/status-markers` | If a lane ships, its marker flips to SHIPPED. If it half-ships, it goes to PARTIAL with a precise reason. No lane flips a marker green that does not have evidence. |

---

## 2. Sequencing recommendation

Revised 2026-04-27 after reconciling `TODO_V2.md` against
`docs/MASTER_ROADMAP.md`, `docs/BUILD_HISTORY.md`, and the live PVL / Trust /
P5 audits. The Matrix/Tier-2 template set has shipped, the reset gate is
closed, and the remaining near-term work is either evidence-gated or narrowly
scoped cleanup. Pick lanes in this order if no stronger live signal tells you
otherwise:

1. ~~**P3.0** (TODO hygiene)~~ — **SHIPPED** 2026-04-25.
2. ~~**P3.5** (M6 expansion live wiring) — critical path~~ — **SHIPPED**
   2026-04-25 (Tasks 2 & 3; Tasks 1 & 4 deferred per guardrail). Matrix
   ladder is unblocked. Evidence: `p3_5_m6_expansion_wiring-2026-04-25.md`.
3. ~~**P3.6** (`positive_memory` Tier-2 template)~~ — **SHIPPED**
   2026-04-25. Encoder + orchestrator dispatch + schema-audit cleanup.
   CANDIDATE_BIRTH only, no live promotion. Evidence:
   `p3_6_positive_memory-2026-04-25.md`. Template the rest of the
   Tier-2 ladder copies.
4. ~~**P3.1** (TLA+ formal verification)~~ — **SHIPPED** 2026-04-25.
   Spec, cfg, README, and clock-bound soundness fix landed. TLC ran on
   the brain (`duafoo@192.168.1.222`, OpenJDK 21.0.10, tla2tools 2.19,
   32 workers): 9,724,247 states generated, 2,528,932 distinct,
   diameter 34, 0 counterexamples, 3 s wall. Brain-side schema audit
   (0 violations), truth probe (0 findings), and validation pack
   (HRR/P5/runtime guardrails all PASS) confirm production behavior
   unchanged. Evidence: `tla_phase_65-2026-04-25.md`.
5. ~~**P3.2** (`user_correction` belief writer)~~ - **SHIPPED**
   2026-04-24 during the TODO_V2 cleanup + unblock sprint. Writer lives at
   `GraphBridge.create_user_correction_link`; `FUTURE_ONLY_EVIDENCE_BASES`
   is intentionally empty. Evidence: `reset_gate_status-2026-04-24.md`.
6. ~~**P3.3** (`depends_on` prerequisite writer)~~ - **SHIPPED**
   2026-04-24 during the same sprint. Writer lives at
   `GraphBridge.create_prerequisite_link`; `FUTURE_ONLY_EDGE_TYPES` is
   intentionally empty. Evidence: `reset_gate_status-2026-04-24.md`.
7. ~~**P3.4** (Language kernel exit-PRE-MATURE machinery)~~ - **SHIPPED**
   2026-04-24. `/api/language-kernel` reports a registered artifact and the
   `phase_e_language_kernel_identity` marker dynamically flips to SHIPPED
   when `status=registered`, `total_artifacts>=1`, and `live_artifact` is
   present. Evidence: `reset_gate_status-2026-04-24.md`.
8. ~~**P3.16 / P5 continuity cleanup**~~ - **SHIPPED** 2026-04-27.
   Bounded removed-entity retention, explicit active/removed counts, active
   canvas display contract, Pi scalar fallback, and focused regressions landed.
   P5 remains PRE-MATURE, shadow-only, derived-only, and zero-authority.
   Evidence: `p3_16_p5_scene_continuity_cleanup-2026-04-27.md`.
9. **P3.11 feasibility** (`dream_synthesis` Tier-2 - see item 10). Re-run
   only when dream-validator label quality changes.
10. **P3.11** (`dream_synthesis` Tier-2 promotion). Phase 1 feasibility ran
   2026-04-25: verdict **INSUFFICIENT, DEFER**. Live label ledger has 369
   samples (gate 1 OK) but `quarantined` has 0 samples and the
   `reason_distribution` is 100% `uncategorized`. Implementation is BLOCKED
   until the dream validator emits at least one quarantined sample and at
   least three non-zero reason categories. Re-run
   `brain/scripts/dream_synthesis_feasibility.py` to re-check.
11. **P3.12** (Long-horizon attention). Use case is pinned
   (`medication_refill_followup`) but code should still wait until the
   operator explicitly chooses this lane over P5 continuity cleanup.
12. **P3.13** (IntentionResolver Stage 1). **Deferred** until Stage 0
   graduation gates clear. Current live state remains below threshold:
   `backed_commitments_logged=0/30`, `recent_resolutions=0/5`,
   `resolution_histogram_variance=0/2`. Do not start.
13. ~~**P3.14** (Pi `/mind` consolidation: merge mental-world signals into the consciousness particle visualizer; delete duplicate kiosk surface)~~ - **SHIPPED** 2026-04-25. Strictly subtractive on the Pi (deleted `pi/ui/hrr_scene_view.py`, `pi/ui/static/hrr_scene.{html,css}`, `brain/tests/test_pi_hrr_scene_view.py`, mount block in `pi/main.py`); minimal additive on the brain feed (`_build_scene_block` helper + one line in `_build_consciousness_feed`); minimal additive on the Pi visualizer (`consciousness.scene` sub-object + four small render hooks: connection-line reach, cleanup-accuracy companion ring, aurora drift, side-effect canary halo). Wire contract: seven bounded scalars (`enabled`, `entity_count`, `relation_count`, `cleanup_accuracy`, `relation_recovery`, `similarity_to_previous`, `spatial_hrr_side_effects`); no entities, no relations, no vectors, no authority flags. New regression: `brain/tests/test_consciousness_feed_scene_block.py`. Operator scene-graph view stays on the brain dashboard `/hrr-scene` - single source of truth. Evidence: `docs/validation_reports/p3_14_pi_mind_merge-2026-04-25.md`.

**Live observation (2026-04-25, post-P3.14 restart).** First captured
instance of *addressee overreach* in a multi-party room conversation:
JARVIS engaged on a side conversation about contractor payments under
`gesture=leaning_away`, `attention.speaker_confidence=0.411`,
`identity_state.confidence=0.618`, no second-person construction. Content
of the response was correct (perception mature); the social judgment was
not (inhibition immature). Logged as evidence for the eventual P3.8
`speaker_profile` promotion gate; **not** a bug, **not** a wake-reliability
lane (still §4-out-of-scope), **not** a code change tonight. Watch
conjunction + repetition policy in
`docs/validation_reports/emergent_addressee_overreach_observation-2026-04-25.md`
§9. If three or more such instances accumulate within a 7-day rolling
window, escalate to a real engineering lane (provisional name **P3.15 —
Addressee inhibition gate for soft-signal multi-party context**).

This is a recommendation, not a contract. Any single lane can be picked and
shipped in isolation as long as it honors §1. The P3.5 hard dependency on
P3.6-P3.11 is satisfied; the five-lane Tier-2 template set
(P3.6 `positive_memory`, P3.7 `negative_memory`, P3.8 `speaker_profile`,
P3.9 `temporal_pattern`, P3.10 `skill_transfer`) all shipped 2026-04-25
and demonstrate the template for any future Tier-2 lane — copy any
of `positive_memory_encoder.py`, `negative_memory_encoder.py`,
`speaker_profile_encoder.py`, `temporal_pattern_encoder.py`, or
`skill_transfer_encoder.py`, add a focus dispatch in
`_matrix_focus_signal()`, and remove the future-only whitelist entry
once the writer literal ships.

**PVL / Trust dashboard note (2026-04-27).** The Trust dashboard's "L1
pending" count and the outcome scheduler's "pending" count are different
metrics, not a contradiction: unresolved ledger entries versus scheduled
delayed checks. PVL wiring is active, but coverage remains evidence-gated by
awaiting/failing contracts in skill learning, Matrix protocol, language
evidence floors, system-upgrade lifecycle, and policy experience volume. Do
not turn those into code lanes unless a concrete contract regresses.

---

## 3. Lane catalog

Each lane below has the same shape: scope, files touched, architecture
guardrails (the things this lane must not do), acceptance criteria, and the
evidence artifact path.

**Trace-validation pass (2026-04-24).** Every lane below was trace-validated
against the local source tree and the live brain on `192.168.1.222`.
Evidence: `docs/validation_reports/todo_v2_trace_validation-2026-04-24.md`.
Sixteen discrepancies found and corrected in place; two real code bugs
surfaced in P3.5 (shadow-signal plumbing, `specialist_verification_ts`
write-site) and were promoted from "audit" to "real fix required". P3.13
deferred. Everything else confirmed as written.

### P3.0. TODO.md Priority 6 hygiene flip — SHIPPED 2026-04-25

**Status.** SHIPPED. `TODO.md:903-934` already reconciled against shipped
reality at the time of TODO_V2 trace-validation: section banner reads
"SHIPPED in P1.4 (commit `2fe404e`)" (TODO.md:903), every scope-list
checkbox is `[x]` with a `*(shipped 2fe404e)*` annotation
(TODO.md:925-934), and the historical-note paragraph at TODO.md:916-921
records the trace-validation provenance. Lane closed via documentation-only
hygiene; no code changes required.

**Scope.** Reconcile `TODO.md` lines 903 to 932 against shipped reality.
`IntentShadowRunner` is fully shipped (`brain/reasoning/intent_shadow.py`, 753
LOC exact per `wc -l`, wired into `tool_router._finalize` at line 1262,
dashboard panel in `self_improve.html:450-494`, tests in
`brain/tests/test_intent_shadow.py`, shipping commit `2fe404e`).

**Files touched.** `TODO.md` only.

**Architecture guardrails.** None. This is documentation hygiene.

**Acceptance criteria.**
- All eight boxes at the lines above flipped to `[x]` with a `2fe404e`
  commit reference (confirmed by `git log brain/reasoning/intent_shadow.py`).
- Section banner replaced with "shipped in P1.4 at 2fe404e" reference.
- Section retained for historical reference, not deleted.

**Evidence artifact.** None. Mention in commit message.

---

### P3.1. TLA+ formal verification of Phase 6.5 invariants — SHIPPED 2026-04-25

**Status.** SHIPPED. The TLA+ spec (`docs/formal/phase_65.tla`),
TLC config (`docs/formal/phase_65.cfg`), and reproduction README
(`docs/formal/README.md`) are committed and consistent with the four
Python anchor tests. A clock-bound soundness fix was applied
(relax `TypeOK` `clock` from a finite interval to `Nat`, add
`StateBound == clock <= MAX_AUDIT_LEN * 4`, wire as `CONSTRAINT` in the
cfg, drop the now-redundant guard in `RequestEscalation`). TLC ran on
the brain authority host (`duafoo@192.168.1.222`, OpenJDK 21.0.10,
tla2tools 2.19, 32 workers / 32 cores): **9,724,247 states generated,
2,528,932 distinct states found, diameter 34, 0 counterexamples,
3 s wall**. All TLC INVARIANTS (`TypeOK`, `NoAutoPromotion`,
`AttestationImmutability`, `RequestOkDerivationInv`, `SafetyInvariants`)
hold over the bounded state space, and the three structurally-enforced
invariants (`CurrentOkIsLiveSourced`, `EvidenceClassSeparation`,
`AuditLogAppendOnly`) are confirmed by the absence of any reachable
state that would only be reachable via a forbidden assignment.
Brain-side `schema_emission_audit.py` (0 violations),
`dashboard_truth_probe.py` (0 findings), and `run_validation_pack.py`
(every HRR/P5/runtime guardrail PASS) confirm that this
documentation-only lane has not regressed production behavior.

**Evidence.** `docs/validation_reports/tla_phase_65-2026-04-25.md`
(spec landing + soundness fix + TLC stdout + state-space summary +
brain-side audit record + per-invariant Python cross-reference table).

**Scope.** Express the six Phase 6.5 invariants as a TLA+ specification and
model-check them with TLC. The Python tests at `tests/test_l3_escalation.py`,
`tests/test_l3_promotion_invariant.py`, `tests/test_autonomy_audit_ledger.py`,
and `tests/test_l3_snapshot_caches.py` collectively assert these invariants
by example. The TLA+ spec proves them by exhaustive model checking over a
bounded state space.

**Files touched.**
- `docs/formal/phase_65.tla` (new)
- `docs/formal/phase_65.cfg` (new)
- `docs/formal/README.md` (new, brief)
- No source code changes. Spec is documentation-grade.

**State variables.** `level`, `current_ok`, `prior_attested_ok`,
`attestation_strength`, `request_ok`, `approval_required`, `activation_ok`,
`audit_log` (sequence).

**Actions.** `RequestEscalation`, `Approve`, `Reject`, `Apply`, `MarkClean`,
`MarkRolledBack`, `Expire`, `RotateLedger`, `RestartProcess`.

**Safety properties to prove.**
1. `CurrentOkIsLiveSourced`. No action assigns `current_ok` from the ledger or attestation record.
2. `NoAutoPromotion`. No action sequence transitions `level` from 2 to 3 without a preceding `Approve`.
3. `EvidenceClassSeparation`. `current_ok`, `prior_attested_ok`, and `activation_ok` are never unified by any action.
4. `AuditLogAppendOnly`. `audit_log[i]` is never mutated after write; `Len(audit_log)` is non-decreasing except under `RotateLedger`.
5. `AttestationImmutability`. Attestation records are never edited after acceptance.
6. `RequestOkDerivation`. `request_ok = current_ok \/ prior_attested_ok` always.

**Liveness (optional).** Every valid request eventually terminates in one of `{clean, rolled_back, rejected, expired}`.

**Architecture guardrails.**
- The spec mirrors the code. If the spec disagrees with the code, the
  spec is wrong (this release). File a separate audit.
- Bounded state: `level \in {1, 2, 3}`, `attestation_strength \in {none, archived_missing, verified}` (matches `brain/autonomy/attestation.py:71-72` and `tests/test_l3_snapshot_caches.py::test_archived_missing_strength_distinguished_from_verified`), approval horizon at most 4 steps. Larger bounds are future work.

**Acceptance criteria.**
- TLC run completes with zero counterexamples on all six safety properties.
- TLC log saved as evidence.
- `docs/formal/README.md` explains how to reproduce the run.

**Evidence artifact.** `docs/validation_reports/tla_phase_65-YYYY-MM-DD.md` with TLC stdout + the cfg used + state-space summary.

---

### P3.2. `user_correction` belief-graph evidence writer — SHIPPED 2026-04-24

**Status.** SHIPPED during the TODO_V2 cleanup + unblock sprint. The writer
surface is `BeliefGraph.on_user_correction()` ->
`GraphBridge.create_user_correction_link()`, which emits a `contradicts` edge
with `evidence_basis="user_correction"`. The schema-emission audit now keeps
`FUTURE_ONLY_EVIDENCE_BASES` intentionally empty; this lane is no longer open.
Evidence: `docs/validation_reports/reset_gate_status-2026-04-24.md`.

**Historical scope.** `brain/scripts/schema_emission_audit.py` previously
whitelisted `user_correction` as `FUTURE_ONLY` because no live
operator-correction surface emitted graph edges with that evidence base. The
evidence base is consumed by the decay-immunity gate at
`brain/epistemic/belief_graph/edges.py:226` (propagation / integrity /
topology instead consume `depends_on` - see P3.3). This lane shipped the
writer and removed the whitelist entry.

**Files touched.**
- `brain/epistemic/calibration/correction_detector.py` (or wherever user corrections are first detected). Emit the edge.
- `brain/epistemic/belief_graph/edges.py`. Confirm the existing API supports `user_correction`; no new edge type.
- `brain/scripts/schema_emission_audit.py:70-74`. Remove whitelist entry.
- `brain/tests/test_belief_graph.py`. Add coverage that a user correction produces a `user_correction`-bearing edge.

**Architecture guardrails.**
- The writer must hash the operator-corrected statement and store the hash, not the raw text, when belief-graph privacy applies.
- No new edge *type*. `user_correction` is an *evidence base* on existing edge types.
- Writer must respect the existing `BeliefEdge` API. No bypass.
- Schema audit must continue to pass with zero violations after the whitelist entry is removed.

**Acceptance criteria.**
- A simulated user correction in a test produces a belief graph edge whose evidence base contains `user_correction`.
- `scripts/schema_emission_audit.py` reports zero violations and zero `future_only` for `user_correction`.
- 4158+ regression baseline holds.

**Evidence artifact.** `docs/validation_reports/p3_2_user_correction_writer-YYYY-MM-DD.md` with the audit before/after diff and a sample edge payload.

---

### P3.3. `depends_on` prerequisite-tracking writer — SHIPPED 2026-04-24

**Status.** SHIPPED during the TODO_V2 cleanup + unblock sprint. The writer
surface is `BeliefGraph.on_prerequisite_detected()` ->
`GraphBridge.create_prerequisite_link()`, which emits `depends_on` edges with
cycle prevention. The schema-emission audit now keeps
`FUTURE_ONLY_EDGE_TYPES` intentionally empty; this lane is no longer open.
Evidence: `docs/validation_reports/reset_gate_status-2026-04-24.md`.

**Historical scope.** `brain/scripts/schema_emission_audit.py` previously
whitelisted `depends_on` as `FUTURE_ONLY` because no writer emitted it.
Propagation, integrity, and topology already consumed it. This lane shipped
the prerequisite writer surface and removed the whitelist entry. Sequenced
*after* P3.2 per the source comment.

**Files touched.**
- `brain/epistemic/belief_graph/prerequisite_tracker.py` (new, single class).
- `brain/epistemic/belief_graph/edges.py`. Confirm `depends_on` is a registered edge type.
- `brain/scripts/schema_emission_audit.py:76-84`. Remove whitelist entry.
- `brain/tests/test_prerequisite_tracker.py` (new).
- `brain/consciousness/consciousness_system.py`. Add a slot for the tracker tick (cadence to be decided, suggest 60s).

**Architecture guardrails.**
- The dependency-inference policy must be *explicit*. No ML inference in v1.
  Use deterministic heuristics: explicit references, intention chains, commitment lineage.
- Tracker is read-only against the belief graph until it has a writer surface,
  then write-only for the `depends_on` edge type. No mutations elsewhere.
- Deduplication by `(source_node, target_node)` is mandatory. No duplicate edges per cycle.
- Tracker must not enter a cycle (depth limit, visited set).

**Acceptance criteria.**
- A simulated intention chain produces a `depends_on` edge between the dependent and prerequisite.
- Schema audit reports zero violations and zero `future_only` for `depends_on`.
- Cycle test (A depends on B depends on A) does not produce duplicate edges and does not infinite-loop.
- 4158+ regression baseline holds.

**Evidence artifact.** `docs/validation_reports/p3_3_prerequisite_writer-YYYY-MM-DD.md`.

---

### P3.4. Language kernel exit-PRE-MATURE machinery — SHIPPED 2026-04-24

**Status.** SHIPPED during the TODO_V2 cleanup + unblock sprint. The language
kernel seed registration path is live: `/api/language-kernel` can report
`status=registered`, and `/api/meta/status-markers` flips
`phase_e_language_kernel_identity` to SHIPPED when
`status=registered`, `total_artifacts>=1`, and `live_artifact` are true.
Hash match is intentionally not part of the marker gate because active Phase C
training can change the live checkpoint. Evidence:
`docs/validation_reports/reset_gate_status-2026-04-24.md`.

**Historical scope.** Two engineering items closed the artifact-identity loop
without requiring Phase D Stage 1 live traffic. The full progression past
PRE-MATURE still requires live runtime evidence and is operator runbook, not
engineering work.

**Files touched.**
- `brain/language/kernel.py`. Verify `register_current_checkpoint()` works on
  an empty seed, decide whether to ship a seed checkpoint.
- `brain/tests/test_language_kernel.py`. Add the rollback-roundtrip test.
- `brain/dashboard/static/self_improve.html`. Confirm the artifact panel
  surfaces a registered seed if one ships, or remains explicit PRE-MATURE if not.

**Two concrete items.**

1. **First-checkpoint registration path.** Decide whether the release ships
  with a seed Phase C checkpoint (commit one empty-but-valid artifact so
  `get_live_artifact()` returns a real ID instead of None on fresh clones)
  or whether PRE-MATURE on fresh clones is intentional. Document the decision.
  If "ship a seed": generate the minimal valid checkpoint, call
  `register_current_checkpoint()` once, verify the registry transitions from
  `{}` to `{"phasec-v1-<hash>": ...}` and the dashboard tab flips from
  "no artifact" to "Stage 0 baseline".

2. **Multi-checkpoint rollback exercise.** Ship a test plus an evidence
  artifact that:
  - Registers checkpoint A.
  - Registers checkpoint B with a content diff.
  - Calls the artifact-level rollback to restore A on disk.
  - Asserts the Phase C checkpoint JSON byte-matches A after rollback.
  - Asserts the `LanguagePromotionGovernor` state is untouched (artifact rollback is orthogonal to per-class promotion levels).

**Architecture guardrails.**
- No bypass of the Phase D guard. Artifact rollback restores the on-disk
  payload; it does not change `LanguagePromotionGovernor` levels. The governor
  continues to enforce shadow/canary/live gating independently.
- No auto-promotion. `register_current_checkpoint` stays an explicit call.
- `ENABLE_LANGUAGE_RUNTIME_BRIDGE=false` and `LANGUAGE_RUNTIME_ROLLOUT_MODE=off`
  remain the defaults.
- Validation pack `Language Runtime Guardrails Safe` must continue to PASS.

**Acceptance criteria.**
- Decision documented in lane commit message and reflected in the dashboard.
- Rollback roundtrip test passes.
- Validation pack guardrails: PASS, `bridge=False, mode=off, unpromoted_live=0, live_red=0`.
- 4158+ regression baseline holds.

**Evidence artifact.** `docs/validation_reports/p3_4_language_kernel_exit_premature_machinery-YYYY-MM-DD.md`.

---

### P3.5. M6 broadcast slot expansion live wiring (SHIPPED 2026-04-25)

**Closeout (2026-04-25).** Tasks 2 and 3 (the two real production
write-site bugs) shipped 2026-04-24 and the unchecked acceptance gates
(regression tests on the desktop venv, 4158+ baseline, schema audit,
dashboard truth probe) are all green as of 2026-04-25. Tasks 1 (encoder
20→22 dim flip) and 4 (live activation evidence artifact) remain
explicitly deferred per the guardrail — Task 1 is the post-shadow
operator-approved promotion lane (out of scope here), Task 4 fires when
`MATRIX_EXPANSION_TRIGGERED` actually emits on the live brain. P3.6–P3.11
are unblocked. Closing report:
`docs/validation_reports/p3_5_m6_expansion_wiring-2026-04-25.md`. The
historical context below is preserved for the lane archaeology.

**Status correction (2026-04-24).** Trace-validation on the live brain
promoted Tasks 2 and 3 from *audit* to *real-fix-required*. Two write-sites
are missing in production code; without them the entire Matrix / Tier-2
promotion ladder (P3.6-P3.11) is dead-ended. This is the highest-leverage
lane in TODO_V2 and must land before any Tier-2 specialist lane is picked
up.

**Scope.** Almost the entire mechanism is shipped at
`brain/hemisphere/orchestrator.py` (constants `EXPANDED_SLOT_COUNT=6`,
`EXPANSION_MIN_PROMOTED=2`, `EXPANSION_MIN_IMPACT=0.05`,
`EXPANSION_STABILITY_DAYS=7`; `expand_broadcast_slots()`,
`contract_broadcast_slots()`, `_check_expansion_trigger()`,
`MATRIX_EXPANSION_TRIGGERED` event). Two real code fixes, one encoder
decision, and one evidence template remain.

**Files touched.**
- `brain/policy/state_encoder.py`. Handle 20 to 22 dim transition cleanly.
- `brain/policy/shadow_runner.py`. Wire `set_hemisphere_signals()` into `self._shadow_encoder` (currently never called).
- `brain/hemisphere/orchestrator.py`. Write `specialist_verification_ts` at the VERIFIED_PROBATIONARY transition (currently zero write sites; reads at `:748, 1416-1417, 1631`). Confirm `specialist_impact_score` keeps its existing write at `:1384`.
- `brain/tests/test_hemisphere.py` and `brain/tests/test_shadow_runner.py`. Add coverage for the shadow signal plumbing, the verification-ts write-once, and `_check_expansion_trigger()` passing when all three conditions are satisfied.
- `brain/dashboard/static/showcase.html`. Small reactive note when M6 fires (the cockpit `renderers.js:2302-2307` already reacts; showcase does not).

**Four tasks.**

1. **Policy state encoder dimension switch.** Decide between (a) pad to 22 dims
  from day one and feed zeros to unused slots (simpler, recommended) or
  (b) trigger a policy-NN retrain when M6 fires (more correct but invasive).
  Document the decision in commit message.

2. **Shadow-signal plumbing fix (real bug).** `ShadowStateEncoder` reads
  `slot_4` / `slot_5` correctly (`state_encoder.py:211-213`), but
  `ShadowPolicyRunner` never calls `set_hemisphere_signals()` on
  `self._shadow_encoder`. Only the live encoder is wired
  (`consciousness/engine.py:161, 738`). During M6 A/B, dims 16-21 are
  therefore all 0.0 and the shadow policy cannot learn whether expanded
  slots help. Fix: mirror the live encoder's signal feed into the shadow
  encoder at every tick where signals refresh. Add a regression test.

3. **`specialist_verification_ts` write-site (real bug).** The field is
  declared at `brain/hemisphere/types.py:223` (default `0.0`) and read at
  `orchestrator.py:748, 1416-1417, 1631`. **It has zero write sites in
  the entire repo.** Consequence: the guard at `orchestrator.py:1632`
  always fails (`min(...) == 0.0`), so `_check_expansion_trigger()` can
  never succeed. Separately, the `PROBATIONARY_TRAINING →
  VERIFIED_PROBATIONARY` transition at `:748` is also blocked. Fix: set
  `specialist_verification_ts = time.time()` at the VERIFIED_PROBATIONARY
  transition (write-once), and add a regression test that confirms both
  the transition and `_check_expansion_trigger()` can fire.

4. **Activation evidence artifact.** When M6 actually fires on the live brain,
  emit a validation report with the three trigger conditions, the promoted
  specialist list, mean impact, stability window, and the new slot
  assignments. This lane writes the *template* for that artifact even if
  the live trigger has not fired yet.

**Architecture guardrails.**
- The shadow path stays shadow until live promotion is operator-approved.
  This lane does not flip slot_4 / slot_5 into the *live* policy state encoder
  read; that is the post-shadow promotion lane (out of scope here).
- No bypass of `EXPANSION_MIN_IMPACT` or `EXPANSION_STABILITY_DAYS`. If the
  thresholds need tuning later, that is a separate decision with its own
  evidence.
- Hysteresis (`SLOT_SWAP_THRESHOLD=1.15`, `SLOT_MIN_DWELL_CYCLES=3`) must
  apply to the new slots identically.
- `specialist_verification_ts` is write-once at the VERIFIED_PROBATIONARY
  transition. No other code path may mutate it. Rolling-back a specialist
  back to PROBATIONARY_TRAINING and re-promoting must re-stamp a new ts
  (this is the only legal "write twice" path and must be tested).

**Acceptance criteria.**
- All four tasks ship.
- New regression tests: (a) shadow encoder receives slot_4/slot_5 during
  A/B evaluation; (b) `specialist_verification_ts` is written exactly
  once at the VERIFIED_PROBATIONARY transition; (c) synthetic
  `_check_expansion_trigger()` passes with ≥2 PROMOTED, mean impact ≥0.05,
  oldest verification_ts older than 7 days.
- 4158+ regression baseline holds.

**Evidence artifact.** `docs/validation_reports/p3_5_m6_expansion_wiring-YYYY-MM-DD.md`
with the before/after write-site diff, the three regression test outputs, and
the synthetic `_check_expansion_trigger()` pass.

---

### P3.6 to P3.10. Five Tier-2 Matrix specialists

**Closeout — P3.6 (2026-04-25).** `positive_memory` shipped as the
template for the rest of the Tier-2 ladder. Encoder lives at
`brain/hemisphere/positive_memory_encoder.py`; orchestrator dispatch
at `_matrix_focus_signal()` bypasses the accuracy-as-proxy fallback
for Tier-2 focuses with a registered encoder. CANDIDATE_BIRTH only,
no canonical writes, schema audit at 0 violations, dashboard truth
probe at 0 findings, 119/119 hemisphere + Matrix tests green
(including 19 new P3.6 regressions). Closing report:
`docs/validation_reports/p3_6_positive_memory-2026-04-25.md`.

**Closeout — P3.7 (2026-04-25).** `negative_memory` shipped as the
natural pair to `positive_memory` and the second Tier-2 specialist
template instantiation. Encoder lives at
`brain/hemisphere/negative_memory_encoder.py`; orchestrator dispatch
extended at `_matrix_focus_signal()` with a `NEGATIVE_MEMORY` branch
plus `_build_negative_memory_context()` (reads `memory_storage`,
`_tier1_failure_counts`, and `epistemic.quarantine.pressure`). Block
C deliberately weighted higher than P3.6 (0.30 vs 0.20) so the signal
does not starve under low-conversation periods — direct fix for the
`emotion_depth` watch item. No emotion_depth consumption (regression-
tested). CANDIDATE_BIRTH only, no canonical writes, schema audit at 0
violations, dashboard truth probe at 0 findings, 113/113 hemisphere +
Matrix tests green (including 21 new P3.7 regressions), 4485/4485
full sweep (excluding 6 pre-existing unrelated flakes). Closing
report: `docs/validation_reports/p3_7_negative_memory-2026-04-25.md`.

**Closeout — P3.8 (2026-04-25).** `speaker_profile` shipped as the
third Tier-2 specialist template instantiation. Encoder lives at
`brain/hemisphere/speaker_profile_encoder.py`; orchestrator dispatch
extended at `_matrix_focus_signal()` with a `SPEAKER_PROFILE` branch
plus `_build_speaker_profile_context()` (reads
`IdentityFusion.get_status()` via `IdentityResolver._fusion`,
`IdentityResolver.get_known_names()`, and the soul-service
relationship registry size). Load-bearing **no-raw-embedding-leak**
boundary enforced at three layers (encoder input shape, orchestrator
context builder, and two regression tests — one behavioural, one
static). Block A leads at 0.50 (current-speaker confidence is the
canonical signal); Block B at 0.30 (addressee disambiguation is
operationally critical); Block C at 0.20. CANDIDATE_BIRTH only, no
canonical writes, no `emotion_depth` consumption, schema audit at 0
violations (emitted-focus count 20 → 21), dashboard truth probe at 0
findings, 428/428 hemisphere + Matrix + audit-regression tests green
(including 23 new P3.8 regressions), 4508/4508 full sweep (excluding
the same 6 pre-existing unrelated flakes seen in P3.6 / P3.7).
Closing report:
`docs/validation_reports/p3_8_speaker_profile-2026-04-25.md`.

**Field-observation evidence (2026-04-25, post-restart).** First captured
instance of *addressee overreach* under exactly the soft-signal stack
this specialist is meant to learn from. Conversation
`9befc079-8c15-4079-b760-01b7f01fe5b1` at 15:11 EDT: wake fired during a
multi-party room conversation about contractor payments;
`gesture=leaning_away` (conf 0.5), `attention.speaker_confidence=0.411`,
`identity_state.confidence=0.618`, no second-person construction in the
transcript, `tool_route=MEMORY` with `memories_retrieved.count=0`. JARVIS
correctly reasoned about the overheard math (perception mature) but
should have stayed quiet (inhibition immature). No autonomy / authority /
schema / HRR / P5 impact. This is *evidence* for the eventual P3.8
promotion gate, not a bug. Recorded in
`docs/validation_reports/emergent_addressee_overreach_observation-2026-04-25.md`
with a §9 conjunction-trigger watch list. Repetition policy: if the same
conjunction recurs three or more times within a 7-day rolling window,
escalate to a real engineering lane (provisional name **P3.15 —
Addressee inhibition gate for soft-signal multi-party context**); until
then, observe only.

**Closeout — P3.9 (2026-04-25).** `temporal_pattern` shipped as the
fourth Tier-2 specialist template instantiation. Encoder lives at
`brain/hemisphere/temporal_pattern_encoder.py`; orchestrator dispatch
extended at `_matrix_focus_signal()` with a `TEMPORAL_PATTERN` branch
plus `_build_temporal_pattern_context()` (reads `memory_storage`
timestamps for activity counts/recency and `mode_manager.get_state()`
for mode dwell / transition counts). Load-bearing
**no-schedule-claim** privacy boundary enforced at three layers
(encoder input shape, orchestrator context builder, and three
regression tests — one behavioural, one static source-scan, one
orchestrator-context fence). Block A leads at 0.50 (recency / density
is the canonical "is this temporal context familiar?" signal); Block
B at 0.30 (mode-stability is more directly observable than gap-CV
smoothness at low volume); Block C at 0.20. CANDIDATE_BIRTH only, no
canonical writes, no `emotion_depth` consumption, no hour-of-day /
weekday / schedule fields, schema audit at 0 violations
(emitted-focus count 21 → 22; only `skill_transfer` remains in the
Tier-2 future-only whitelist), dashboard truth probe at 0 findings,
452/452 hemisphere + Matrix + audit-regression tests green
(including 24 new P3.9 regressions), 4532/4532 full sweep
(excluding the same 6 pre-existing unrelated flakes). Closing
report: `docs/validation_reports/p3_9_temporal_pattern-2026-04-25.md`.

**Closeout — P3.10 (2026-04-25).** `skill_transfer` shipped as the
fifth and final Tier-2 specialist template instantiation, completing
the planned five-lane Tier-2 Matrix encoder set. Encoder lives at
`brain/hemisphere/skill_transfer_encoder.py`; orchestrator dispatch
extended at `_matrix_focus_signal()` with a `SKILL_TRANSFER` branch
plus `_build_skill_transfer_context()` (reads
`skill_registry.get_status_snapshot()` for registry breadth /
maturity / matrix-protocol fraction and learning-job counts —
`LearningJobOrchestrator` is `main.py`-owned, encoder degrades
cleanly without it). Load-bearing
**similarity-is-not-capability** boundary enforced at five layers
(encoder input shape, orchestrator context builder, three
regression tests — one behavioural, one static source-scan, one
orchestrator-context fence — plus a dedicated
fake-verified-skill invariance test and a canonical-mutator scan
covering `skill_registry.{set_status,verify,promote,…}`,
`capability_gate.{allow,set,grant,promote}`,
`set_capability`, `promote_capability`, `verify_skill`). Block A
leads at 0.50 (registry breadth is the canonical "is the skill
ecosystem rich enough for transfer to be meaningful?" signal);
Block B at 0.30 (learning-job phase progress is more directly
observable than diversity heuristics); Block C at 0.20.
CANDIDATE_BIRTH only, no canonical writes, no skill_registry /
capability_gate mutators referenced, no promote-this-skill /
verify-now / capability-proven hint consumption, schema audit at
0 violations (emitted-focus count 22 → 23; only `custom` and
`hrr_encoder` remain in the whitelist, neither in the Tier-2
ladder), dashboard truth probe at 0 findings, 196/196
hemisphere + Matrix + per-lane Tier-2 tests green (including 24
new P3.10 regressions), 4556/4562 full sweep (excluding the same
6 pre-existing unrelated flakes — dogfood acquisition plan-review,
two Pi proxy mocking tests, three research enrichment tests).
Closing report:
`docs/validation_reports/p3_10_skill_transfer-2026-04-25.md`.

The Tier-2 Matrix encoder template set is now complete. A
separate Tier-2 Matrix template closeout report
(`docs/validation_reports/p3_tier2_matrix_template_closeout-2026-04-25.md`)
will summarise readiness across all five lanes before P3.11
feasibility.

**Scope.** Five Tier-2 specialists declared in `brain/hemisphere/types.py`. Build
them. Each one is the same template applied to a different focus. P3.6 through
P3.10 ship the full template; the Tier-2 Matrix encoder set is now complete and
no Tier-2 lane remains in the `FUTURE_ONLY_HEMISPHERE_FOCUSES` whitelist at
`brain/scripts/schema_emission_audit.py`.

| Lane | Focus | Source signal candidates | Status |
| --- | --- | --- | --- |
| P3.6 | `positive_memory` | reinforced episodic recall, valence-positive memory writes, policy delta-positive | **SHIPPED** 2026-04-25 |
| P3.7 | `negative_memory` | corrected memory entries, regression-flagged outcomes, friction-mined misses, quarantine pressure, contradiction debt | **SHIPPED** 2026-04-25 |
| P3.8 | `speaker_profile` | identity-fusion confidence (voice + face), addressee context, multi-speaker stability, known-speaker registry, soul-service relationship count — **no raw embeddings** | **SHIPPED** 2026-04-25 |
| P3.9 | `temporal_pattern` | activity recency / density (memory timestamps), mode dwell / transition counts, cadence CV — **no schedule / calendar / weekday / hour-of-day claims** | **SHIPPED** 2026-04-25 |
| P3.10 | `skill_transfer` | skill registry breadth & maturity, learning-job phase distribution, cross-skill diversity / overlap — **similarity is not capability**; no skill_registry / capability_gate mutators referenced | **SHIPPED** 2026-04-25 |

**Per-lane template (same for all five).**

**Files touched per lane.**
- `brain/hemisphere/{focus}_encoder.py` (new). Defines the input feature vector.
- `brain/hemisphere/types.py`. Confirm focus is in `MATRIX_ELIGIBLE_FOCUSES` and has a `DistillationConfig`.
- `brain/hemisphere/architect.py`. Register the focus in the architect's spawn list with a default topology.
- `brain/hemisphere/orchestrator.py`. Confirm the focus participates in broadcast-slot competition (Tier-2 path, not Tier-1).
- `brain/scripts/schema_emission_audit.py:90-99`. Remove the focus from `FUTURE_ONLY_HEMISPHERE_FOCUSES` once a writer ships.
- `brain/tests/test_hemisphere.py` (or per-focus test file). Encoder shape, lifecycle stage entry, broadcast-slot competition.

**Architecture guardrails (per lane).**
- New specialist enters at `SpecialistLifecycleStage.CANDIDATE_BIRTH`
  (see `brain/hemisphere/types.py:192-199`). The actual progression is
  `CANDIDATE_BIRTH → PROBATIONARY_TRAINING → VERIFIED_PROBATIONARY →
  BROADCAST_ELIGIBLE → PROMOTED` (with `RETIRED` as the terminal state).
  Each transition honors the existing gate; promotion to PROMOTED requires
  beating the incumbent on validation with the configured dwell. No shortcut.
- No specialist may write to canonical state. The Tier-2 specialist is a
  signal source for the policy state encoder; it is not an authority on
  memory, identity, or conversation.
- The signal value returned by `_compute_signal_value()` for the new focus
  must be a real-time inferable scalar in `[0, 1]`. The existing
  accuracy-as-proxy fallback at `orchestrator.py:1353` (last line of
  `_compute_signal_value`) is a Tier-1 pattern. Tier-2 specialists must
  ensure their inference path always succeeds (feature ingestion must be
  complete before the network is eligible to broadcast); if the fallback
  branch is reached for a Tier-2 focus, the lane treats that as a bug.
- Schema audit must continue to pass with zero violations after the whitelist
  entry is removed.

**Acceptance criteria (per lane).**
- New specialist appears in `/api/self-improve/specialists` (or the equivalent Tier-2 surface) with `lifecycle: candidate_birth` and a non-zero training signal count after the live brain has had time to feed it.
- Schema audit reports zero violations and zero `future_only` for the focus.
- A test exists that spawns the focus, drives feature ingestion, and verifies the lifecycle transitions are gated correctly (at minimum `CANDIDATE_BIRTH → PROBATIONARY_TRAINING` with synthetic signal volume).
- 4158+ regression baseline holds.
- The specialist competes for a broadcast slot using the standard hysteresis (`SLOT_SWAP_THRESHOLD=1.15`, `SLOT_MIN_DWELL_CYCLES=3`).

**Evidence artifact (per lane).** `docs/validation_reports/p3_{6..10}_{focus}-YYYY-MM-DD.md`.

**Coupling to P3.5 (satisfied dependency).** P3.5's Tasks 2 and 3 landed and
were re-verified on 2026-04-25: `specialist_verification_ts` is written at
the VERIFIED_PROBATIONARY transition, and `set_hemisphere_signals()` is wired
through the shadow encoder. P3.6-P3.10 are therefore open for normal
observation and eventual lifecycle progression, but not auto-promotion.

---

### P3.11. `dream_synthesis` Tier-2 promotion

**Phase 1 outcome (2026-04-25): INSUFFICIENT — DEFER.** Phase 2
implementation is BLOCKED on upstream-validator label-quality work
that is not in scope for this lane. The canonical feasibility
script `brain/scripts/dream_synthesis_feasibility.py` returned
`verdict = "INSUFFICIENT"` (exit code 2) against the live
`distill_dream_validator.jsonl` ledger:

- Total labeled samples: 369 (gate 1 `>= 200` PASS).
- `class_distribution`: `promoted=178 (48.24%) / held=177 (47.97%) / discarded=14 (3.79%) / quarantined=0 (0.00%)` — gate 2 (all four `LABEL_CLASSES` populated) **FAIL**.
- Majority class share: 48.24% (gate 3 `<= 80%` PASS).
- `reason_distribution`: `{ "uncategorized": 369 }` — gates 4 (≥3 non-zero reason classes) and 5 (no reason class > 80%) both **FAIL**.
- Tier-1 stability: 1 active network (`distill_de720e6d21`), 0 failures, 0 rollback events, `total_attempts=1`, `evolution_generations=0`, `maturity=stable`. Accuracy plateaued at 0.3212, consistent with the missing-class ceiling.

Re-eligibility requires the dream validator to emit at least one
`quarantined` sample and at least three non-zero `reason_category`
classes (the taxonomy is `coherence_pass`, `novel_insight`,
`redundant`, `low_coherence`, `low_confidence`, `stale`,
`anomalous`, `uncategorized`). Re-run
`brain/scripts/dream_synthesis_feasibility.py` to re-check. Closing
report: `docs/validation_reports/p3_11_dream_synthesis_feasibility-2026-04-25.md`.

**Scope.** The `dream_synthesis` specialist is currently Tier-1 (`16 -> 4`
approximator, `kl_div`, `min_samples=15`). Promote to Tier-2 *only if* the
live signal-count and label-distribution histograms justify it. This lane
starts with a feasibility check and only proceeds to implementation if the
signal is rich enough.

**Phase 1 (feasibility, half a session).** SHIPPED 2026-04-25.
- Pull `/api/self-improve/specialists` for `dream_synthesis`. Done.
- Confirm at least 200 distinct distillation signals. PASS (369 / 200).
- Confirm the `reason_distribution` histogram has at least three non-zero classes with no class above 80 percent of total. **FAIL** (1 non-zero class, 100% in `uncategorized`).
- Document the finding. If criteria fail, defer the lane. **DEFERRED**.

**Phase 2 (implementation, only if Phase 1 passes).** Same template as P3.6 to P3.10:
- Write `brain/hemisphere/dream_synthesis_tier2_encoder.py` that consumes a
  richer state vector (existing 16-dim plus broadcast context, mood, belief-graph
  features). Decide the dim, suggest 32 to 48.
- Add `DREAM_SYNTHESIS` to `MATRIX_ELIGIBLE_FOCUSES` (currently not present
  per `brain/hemisphere/types.py:55-60`).
- Move `DREAM_SYNTHESIS` out of `_TIER1_FOCUSES` (currently at
  `brain/hemisphere/orchestrator.py:98`) so it enters the Tier-2 broadcast
  competition.
- Hook lifecycle entry at `SpecialistLifecycleStage.CANDIDATE_BIRTH` (same as
  P3.6-P3.10).
- Replace the accuracy-as-proxy signal value with a real-time inferable
  dream-coherence or artifact-promotion-probability scalar.

**Files touched.** Same shape as P3.6 to P3.10, plus the Tier-1 to Tier-2 lift
in `brain/hemisphere/types.py` and `brain/hemisphere/orchestrator.py`.

**Architecture guardrails.**
- Same as P3.6 to P3.10.
- Additionally: the existing Tier-1 dream specialist may stay until the Tier-2
  one earns PROMOTED, then the Tier-1 is retired. No simultaneous live
  authority.

**Acceptance criteria.** Same shape as P3.6 to P3.10.

**Evidence artifact.** `docs/validation_reports/p3_11_dream_synthesis_tier2-YYYY-MM-DD.md`.

---

### P3.12. Long-horizon attention (multi-day persistent tasks without voice-latency regression)

**Scope.** Build the persistent-task substrate. The consciousness kernel today
schedules many short cycles (`META_THOUGHT_INTERVAL_S=8`,
`EVOLUTION_INTERVAL_S=90`, etc.) but has no concept of a multi-day persistent
task. This lane introduces one.

**Pre-engineering: pin a use case.** Before any code, write one sentence in
the lane commit message describing the *single concrete use case* this first
version supports. Suggested seed: "Track an open research question across a
week and surface findings on resolution." Without a pinned use case this
lane becomes a tarpit.

**Files touched.**
- `brain/consciousness/persistent_tasks.py` (new). `PersistentTask`
  dataclass, registry, durable persistence.
- `brain/consciousness/consciousness_system.py`. Add `PERSISTENT_TASK_INTERVAL_S` (suggest 600s) and the tick handler.
- `brain/dashboard/app.py`. `/api/persistent-tasks` REST surface.
- `brain/dashboard/static/persistent_tasks.html` (new). Operator visibility surface.
- `brain/tests/test_persistent_tasks.py` (new). Restart-resumption invariant test, voice-latency regression guard test.

**Six concrete items.**

1. `PersistentTask` dataclass with fields: `task_id`, `created_at`, `goal`,
  `sub_goals[]`, `horizon_s`, `last_advanced_at`, `state` (`active | paused |
  completed | abandoned`), `progress_ledger[]`, `evidence_refs[]`. Persisted
  to `~/.jarvis/persistent_tasks/`.
2. Background-cycle scheduler. On tick, pull one eligible task, advance it
  one step, write back to the durable store. Budget-gated: never run when
  kernel mode is `VOICE_ACTIVE` or `CONVERSATION_RUNNING`.
3. Restart-resumption invariant. On boot, load the registry and resume
  in-flight tasks from their last ledger entry. Test that spawn + kill +
  restart resumes correctly.
4. Voice-latency regression guard. Sample tick-latency p95 around persistent-task
  advancement. If p95 grows more than 15 percent above the rolling baseline,
  pause the active task and log a regression event. This is the
  "without voice-latency regression" half and needs teeth.
5. Dashboard surface listing in-flight tasks, progress, horizon, next-advance
  time, regression-paused tasks.
6. REST API: `POST/GET/DELETE /api/persistent-tasks` and `GET /api/persistent-tasks/{id}`.

**Architecture guardrails.**
- Persistent tasks are *not* autonomous actors. They consume planning ticks,
  not capability authority. They cannot grant themselves new capabilities or
  promote themselves above L2.
- Task advancement runs through the existing `tool_router` and `capability_gate`.
  No bypass.
- The voice-latency guard must trigger on the *measured* p95, not on a
  configured threshold alone. Real telemetry, not theatre.
- Durable storage uses atomic write (temp file + rename). No partial writes.
- A persistent task that errors three times in a row is auto-paused, not
  auto-abandoned.

**Acceptance criteria.**
- Single pinned use case implemented end-to-end.
- Restart-resumption test passes.
- Voice-latency guard test passes (synthetic p95 spike triggers a pause).
- Dashboard surface renders in-flight tasks.
- REST API round-trip works.
- 4158+ regression baseline holds.

**Evidence artifact.** `docs/validation_reports/p3_12_long_horizon_attention-YYYY-MM-DD.md`.

---

### P3.13. IntentionResolver Stage 1 — DEFERRED (live gates unmet)

**Status (2026-04-24).** **Deferred.** Trace-validation against the live
brain (`/api/intentions`) shows three of four Stage 0 graduation gates
failing: `backed_commitments_logged=0/30`, `recent_resolutions=0/5`,
`resolution_histogram_variance=0/2`. Only `persistence_error_floor`
(observed=0, required<=5) passes. The lane's own precondition forbids
implementation work until these gates clear through accumulated lived
runtime data. Do not start.

**Scope (for when the gate clears).** Implement the frozen design at
`docs/INTENTION_STAGE_1_DESIGN.md`. All four `TODO(intention_stage_1)`
integration points are pre-anchored in production code. The inert
`DistillationConfig` for the Tier-2 hand-off is reserved at
`brain/hemisphere/types.py:489-498` (commentary block starts at 460).

**Pre-engineering: confirm Stage 0 graduation gate.** Before any code, query
`IntentionRegistry.get_graduation_status()` on the live brain and confirm:
- backed commitments logged: 30 or more
- resolved in last 7 days: 5 or more
- non-zero outcome classes (7 days): 2 or more
- persistence error floor: 5 or fewer

If any of these fail, defer the lane. Do not start implementation.

**Files touched (for when the gate clears).**
- `brain/cognition/intention_resolver.py` (new). Implements the frozen
  interface in §1 of the design doc.
- `brain/cognition/intention_registry.py`. Add `get_recent_resolved_for_resolver()`
  and `attach_resolver_verdict()`.
- `brain/memory/fractal_recall.py:309`. Wire post-resolve hook (read-only).
- `brain/personality/proactive.py:186`. Consume resolver candidates.
- `brain/reasoning/bounded_response.py:401`. Surface pending verdicts as a fact line.
- `brain/consciousness/consciousness_system.py:561`. Add `intention_resolver_tick` (30s cadence, mode-gated via `allowed_cycles`).
- `brain/tests/test_intention_resolver.py` (new).

**`route_many()` — reconciled with the frozen design.** An earlier draft of
this lane demanded fixing a "multi-intent prerequisite bug" in
`tool_router.route_many()`. That method does not exist in
`brain/reasoning/tool_router.py`, and `docs/INTENTION_STAGE_1_DESIGN.md §7a.1`
*explicitly rejects* adding `route_many()` as an NN-Maturity-Flywheel
violation ("Compound speech-act structure must be learned from lived
interaction, not baked into the bootstrap router by fiat"). The requirement
is therefore **removed** from P3.13. Multi-intent handling is out of scope
for Stage 1; it graduates through observation, not by hand-coding.

**Architecture guardrails.**
- Resolver is read-only against `WorldModel` and `IntentionRegistry`.
- Resolver NEVER calls TTS or emits `CONVERSATION_RESPONSE` directly.
- Resolver emits candidates *only* through `ProactiveGovernor`. Every existing
  gate (`AddresseeGate`, `CapabilityGate`, cooldowns) stays in place.
- Resolver verdict log is append-only with size-based rotation (10 MB).
- Promotion ladder rungs each have an automatic rollback trigger.
- Two graduation criteria (PVL coverage and green-weeks) are explicitly marked
  `unknown` and require human operator confirmation. This stays.
- The `intention_delivery` Tier-2 specialist `DistillationConfig` at
  `hemisphere/types.py:489-498` (commented-out inert block inside the
  reserved narrative that begins at line 460) stays inert. Stage 2 is out
  of scope for this lane.

**Acceptance criteria.**
- Resolver implements the frozen interface byte-for-byte.
- All four `TODO(intention_stage_1)` hooks are wired.
- Resolver runs at `shadow_only` and produces verdicts in the JSONL log.
- A test simulates a resolved intention, runs the resolver, and asserts the
  verdict matches one of the ten reason codes.
- ProactiveGovernor receives no new candidate class until the shadow accuracy
  gate clears.
- 4158+ regression baseline holds.

**Evidence artifact.** `docs/validation_reports/p3_13_intention_resolver_stage_1-YYYY-MM-DD.md` with the graduation-gate confirmation, the verdict-log sample, and the resolver shadow metrics snapshot.

---

### P3.14. Pi `/mind` consolidation — SHIPPED 2026-04-25

**Status.** SHIPPED. The standalone `/mind` kiosk view on the Pi was a redundant duplicate of the brain dashboard `/hrr-scene` page: same payload (`/api/hrr/scene`), worse screen real estate, no end-user audience. P3.14 deletes the duplicate and merges the bounded mental-world signal into the Pi's existing consciousness particle visualizer (`pi/ui/static/particles.js`) by riding the existing `consciousness_feed` transport channel.

**Scope.** Strictly subtractive on the Pi + minimal additive on the brain feed builder + minimal additive on the Pi visualizer's existing five particle classes (no new class).

**Files touched.**

* **Brain (additive).** `brain/perception_orchestrator.py` — added `_SCENE_FEED_KEYS` contract tuple, `_build_scene_block` helper that reads `cognition.mental_world.get_state()` and projects to the seven scalars, and one new line in `_build_consciousness_feed` (`"scene": _build_scene_block()`). `brain/tests/test_consciousness_feed_scene_block.py` (new) pins the wire contract.
* **Pi (additive).** `pi/ui/static/particles.js` — added `consciousness.scene` sub-object, feed ingestion in `onConsciousnessData`, and four subtle visual mappings: (1) `relation_count` → `drawConnections()` distance threshold widens by `min(20, relation_count) * 0.8`; (2) `cleanup_accuracy` → companion ring at radius `r*2 + 11` sweeping a fraction of the circle equal to the cleanup score; (3) `similarity_to_previous` → aurora-blob drift speed multiplied by `1 + (1 - similarity) * 0.6` (clamped to `[1.0, 1.6]`); (4) `spatial_hrr_side_effects > 0` → pulsing thin red canary halo at radius `r*4` (zero-authority alarm).
* **Pi (subtractive — deleted).** `pi/ui/hrr_scene_view.py`, `pi/ui/static/hrr_scene.html`, `pi/ui/static/hrr_scene.css`, `brain/tests/test_pi_hrr_scene_view.py`, mount block in `pi/main.py::start_ui_server` (lines 1003-1018 previously).
* **Docs.** `AGENTS.md` (rule 9 in P5 governance block), `docs/plans/p5_internal_mental_world_spatial_hrr.plan.md` (Pi UI rule + architecture diagram + commit-7 entry + file list), `brain/dashboard/static/science.html` (Dashboard & Pi Surfaces list), `docs/validation_reports/p3_14_pi_mind_merge-2026-04-25.md` (this lane's evidence), `TODO_V2.md` (this entry).

**Wire contract.** Exactly seven keys in the new `scene` block: `enabled` (bool), `entity_count` (int), `relation_count` (int), `cleanup_accuracy` (float [0,1]), `relation_recovery` (float [0,1]), `similarity_to_previous` (float [0,1]), `spatial_hrr_side_effects` (int). **No** entity / relation / scene arrays. **No** `vector` / `raw_vector` / `composite_vector` keys. **No** authority flags (`writes_memory`, `writes_beliefs`, `influences_policy`, `influences_autonomy`, `soul_integrity_influence`, `llm_raw_vector_exposure`, `no_raw_vectors_in_api` all stay brain-side).

**Architecture guardrails.**
- Pi runs zero HRR math.
- Brain never pushes vectors to the Pi.
- Pi feed never carries authority flags.
- No edits to `cognition.mental_world`, `cognition.hrr_spatial_encoder`, `library.vsa.*`, the engine hook, or any maturity gate.
- Operator scene-graph view stays on `/hrr-scene` — single source of truth.

**Acceptance criteria.**
- `brain/tests/test_consciousness_feed_scene_block.py` passes (8 cases): exact key set, bounded-scalar types, no entity/relation/vector leaks, no authority leaks, defaults when facade empty, exception resilience, populated forwarding with rounding, partial-metric clamping, non-dict defense.
- Schema audit and dashboard truth probe stay at 0 violations / 0 findings on the brain after sync + restart.
- `/api/hrr/scene` payload unchanged (operator surface preserved).
- Pi LCD renders the consciousness particle visualizer normally; no `/mind` route exists.
- `/api/full-snapshot` still vector-free; `recursive_vector_scan` still clean.

**Evidence artifact.** `docs/validation_reports/p3_14_pi_mind_merge-2026-04-25.md`.

---

### P3.16. P5 scene-continuity aging/dedupe + display-contract cleanup — SHIPPED 2026-04-27

**Status.** SHIPPED 2026-04-27. Bounded removed-entity retention, explicit
active/removed counts, active-canvas dashboard rendering, Pi scalar fallback,
and focused regression coverage landed. This is a cleanup lane for the
canonical scene-continuity feed that P5 faithfully renders; it is not an HRR
authority expansion and not a maturity promotion. Evidence:
`docs/validation_reports/p3_16_p5_scene_continuity_cleanup-2026-04-27.md`.

**Why now.** Live P5 audits showed `/api/hrr/scene` preserving the zero-authority
contract, but the operator-facing scene graph can look like it is repeating
items because the upstream `SceneTracker` keeps many removed or stale entities
inside the canonical `SceneSnapshot` until eviction pressure reaches the
entity cap. P5 is reflecting that input honestly. The next useful work is to
make lifecycle state and display semantics sharper so old objects do not read
as active Matrix-world objects.

**Scope.**
- Audit `brain/perception/scene_tracker.py` lifecycle policy for removed /
  stale entities, ID churn, and eviction ordering.
- Decide whether to age out removed entities earlier, dedupe repeated semantic
  objects more aggressively, or leave canonical retention alone and improve
  the dashboard's active-vs-removed rendering. Prefer the smallest change that
  makes the operator surface truthful.
- Update the brain dashboard `/hrr-scene` display contract so active entities,
  removed entities, and historical relations cannot be confused.
- Add focused tests around entity lifecycle, removed-object retention, and the
  P5 payload's zero-authority / vector-free invariants.

**Files likely touched.**
- `brain/perception/scene_tracker.py`
- `brain/perception/scene_types.py` if lifecycle metadata needs to be surfaced
- `brain/cognition/spatial_scene_graph.py` only if P5 filtering is the chosen
  display-boundary layer
- `brain/dashboard/static/hrr_scene.html` / related static assets
- Focused tests under `brain/tests/`

**Architecture guardrails.**
- P5 remains **PRE-MATURE**, shadow-only, derived-only, and zero-authority.
- No raw vectors in any API, dashboard, Pi feed, LLM prompt, or history payload.
- No writes to memory, beliefs, identity, policy, autonomy, or soul integrity.
- Do not hide canonical truth to make the dashboard look cleaner. If removed
  entities are retained, label them honestly; if they are aged out, document
  the lifecycle rule and prove it with tests.
- Pi visualizer contract stays seven bounded scalars only.

**Acceptance criteria.**
- A focused lifecycle test proves repeated observations do not create misleading
  duplicate live objects.
- A focused aging/removal test proves removed entities are either retained with
  explicit non-active status or aged out by a documented rule.
- `/api/hrr/scene` remains vector-free and authority flags remain all false.
- Dashboard rendering distinguishes active scene contents from removed/history
  contents.
- Schema audit, dashboard truth probe, and relevant HRR/P5 validation-pack
  checks stay green.

**Evidence artifact.** `docs/validation_reports/p3_16_p5_scene_continuity_cleanup-YYYY-MM-DD.md`.

---

## 4. Out of scope (refuse if asked)

These were explicitly rejected during release planning. Do not pick them up
as part of TODO_V2 lanes.

- **Destructive brain wipe / reset-ceremony as a release prerequisite.** Stays DEFERRED / OPTIONAL.
- **Cryptographic signing of attestations.** Hash-attested operator-seeded is the current trust model.
- **L3 auto-promotion of any kind.** Manual operator action only.
- **Global widening of `ALLOWED_PATHS`.** Per-lane additions only, with sandbox parity update.
- **Cross-contaminating `ever_ok` for non-L3 capabilities from the attestation ledger.**
- **Wholesale replacement of regex routing by NN.** AGENTS.md Principle #2: shadow then promote, never wholesale.
- **Auto-promotion of any specialist past PROVISIONAL.** Manual review for advisory and primary.
- **Stage 1 or Stage 2 Phase D rollout flips.** That is operator runbook, not engineering work.
- **Wake-word reliability tuning without live histogram evidence.**
- **Push to GitHub.** Local commits only.
- **Conversational chatter in commit messages.** Track file edits as if nothing changed.
- **Hand-waving green markers.** A status marker flips to SHIPPED only with evidence on disk.

---

## 5. How to operate (every lane, same workflow)

1. **Pick one lane.** Read its section in §3 carefully. Read `AGENTS.md` if you have not in the current session. Read the relevant source files end to end before changing anything.
2. **Confirm the architecture guardrails.** If a guardrail is unclear, stop and ask the operator. Do not interpret.
3. **Sync to the desktop brain.** Use `sync-desktop.sh`. The live brain is on `192.168.1.222` as `duafoo`. SSH key is in the connection info.
4. **Implement.** Stay surgical. New files are fine. Touching unrelated files is a smell.
5. **Run the test suite on the desktop venv:**
  ```
  PYTHONPATH=$(pwd) .venv/bin/python -m pytest tests/ -q --tb=line \
  --deselect tests/test_supervisor.py::TestSupervisorIntegration
  ```
  Expected baseline: 4158 passed / 4 pre-existing flakes / 5 skipped / 3 deselected. Net new failures block the lane.
6. **Run the truth audits:**
  ```
  PYTHONPATH=$(pwd) .venv/bin/python brain/scripts/schema_emission_audit.py
  PYTHONPATH=$(pwd) .venv/bin/python brain/scripts/dashboard_truth_probe.py
  ```
  Both must report zero violations / zero findings.
7. **Run the validation pack** if the lane touches autonomy, language, or hemisphere code:
  ```
  PYTHONPATH=$(pwd) .venv/bin/python brain/scripts/run_validation_pack.py
  ```
  `Language Runtime Guardrails Safe` must remain PASS with `bridge=False, mode=off, unpromoted_live=0, live_red=0, blocked=0, live=0`.
8. **Write the evidence artifact** under `docs/validation_reports/` per the lane's spec. Include exact command output, not summaries.
9. **Update `OPEN_SOURCE_RELEASE_TRACKER.md`** by creating a new §10 "Phase 3 — Post-release engineering (TODO_V2 lanes)" on first P3 ship, then adding the lane row there. The file currently ends at §9; §4 is frozen for Phase 1 / Continuity Pivot / Phase 2 history and must not be mutated.
10. **Commit locally.** No push. No conversational chatter in the commit message.
11. **Update this file.** Cross out the shipped lane in §3 with a SHIPPED marker and a commit hash. Do not delete it.

---

## 6. Pointers (ground truth)

- **Architecture invariants.** `AGENTS.md` (root)
- **System overview.** `docs/SYSTEM_OVERVIEW.md`
- **Master roadmap.** `docs/MASTER_ROADMAP.md`
- **Build history.** `docs/BUILD_HISTORY.md`
- **Open-source release tracker.** `docs/OPEN_SOURCE_RELEASE_TRACKER.md`
- **Launch-day verification.** `docs/validation_reports/launch_day_verification-2026-04-24.md`
- **Continuity baseline.** `docs/validation_reports/continuity_baseline_2026-04-23.md`
- **IntentionResolver Stage 1 design (frozen).** `docs/INTENTION_STAGE_1_DESIGN.md`
- **Maturity gates reference.** `docs/MATURITY_GATES_REFERENCE.md`
- **Wake reliability tuning.** `docs/WAKE_RELIABILITY_TUNING.md`
- **First hour as a researcher.** `docs/FIRST_HOUR_AS_A_RESEARCHER.md`
- **Scientific paper (release artifact).** `About_JARVIS_ORACLE_EDITION_SCIENTIFIC_PAPER.md`

---

## 7. Lane status board

| Lane | Title | Status | Commit | Artifact |
| --- | --- | --- | --- | --- |
| P3.0 | TODO.md Priority 6 hygiene flip | **SHIPPED** 2026-04-25 | doc-only | TODO.md:903-934 already reconciled at trace-validation time |
| P3.1 | TLA+ formal verification of Phase 6.5 invariants | **SHIPPED** 2026-04-25 (TLC ran on the brain `duafoo@192.168.1.222`: 9,724,247 states / 2,528,932 distinct / depth 34 / 0 counterexamples / 3s wall; brain-side schema audit, truth probe, and validation pack all clean for HRR/P5/runtime guardrails) | doc-only: 2026-04-25 | `tla_phase_65-2026-04-25.md` + `docs/formal/{phase_65.tla,phase_65.cfg,README.md}` |
| P3.2 | `user_correction` belief-graph evidence writer | **SHIPPED** 2026-04-24 (`GraphBridge.create_user_correction_link`; future-only evidence-base whitelist empty) | code: 2026-04-24 ship | `reset_gate_status-2026-04-24.md` |
| P3.3 | `depends_on` prerequisite-tracking writer | **SHIPPED** 2026-04-24 (`GraphBridge.create_prerequisite_link`; future-only edge-type whitelist empty) | code: 2026-04-24 ship | `reset_gate_status-2026-04-24.md` |
| P3.4 | Language kernel exit-PRE-MATURE machinery | **SHIPPED** 2026-04-24 (`/api/language-kernel` registered artifact; `phase_e_language_kernel_identity` dynamic marker can be SHIPPED) | code: 2026-04-24 ship | `reset_gate_status-2026-04-24.md` |
| P3.5 | M6 broadcast slot expansion live wiring | **SHIPPED** 2026-04-25 (Tasks 2 & 3; Tasks 1 & 4 deferred per guardrail) | code: 2026-04-24 ship; gates green: 2026-04-25 | `p3_5_m6_expansion_wiring-2026-04-25.md` (closing) + `p3_5_m6_expansion_wiring-2026-04-24.md` (ship) |
| P3.6 | Tier-2 specialist: `positive_memory` | **SHIPPED** 2026-04-25 (CANDIDATE_BIRTH only; no live promotion) | code: 2026-04-25 ship | `p3_6_positive_memory-2026-04-25.md` |
| P3.7 | Tier-2 specialist: `negative_memory` | **SHIPPED** 2026-04-25 (CANDIDATE_BIRTH only; no live promotion) | code: 2026-04-25 ship | `p3_7_negative_memory-2026-04-25.md` |
| P3.8 | Tier-2 specialist: `speaker_profile` | **SHIPPED** 2026-04-25 (CANDIDATE_BIRTH only; no live promotion; no raw embedding leakage) | code: 2026-04-25 ship | `p3_8_speaker_profile-2026-04-25.md` |
| P3.9 | Tier-2 specialist: `temporal_pattern` | **SHIPPED** 2026-04-25 (CANDIDATE_BIRTH only; no live promotion; no schedule / calendar / weekday claims) | code: 2026-04-25 ship | `p3_9_temporal_pattern-2026-04-25.md` |
| P3.10 | Tier-2 specialist: `skill_transfer` | **SHIPPED** 2026-04-25 (CANDIDATE_BIRTH only; no live promotion; similarity-is-not-capability boundary enforced at five layers) | code: 2026-04-25 ship | `p3_10_skill_transfer-2026-04-25.md` |
| P3.11 | `dream_synthesis` Tier-2 promotion | **DEFERRED** 2026-04-25 (Phase 1 feasibility INSUFFICIENT — `quarantined` class has 0 samples; reason taxonomy 100% `uncategorized`; Tier-1 stable but accuracy plateaued at the missing-class ceiling) | feasibility ran 2026-04-25 | `p3_11_dream_synthesis_feasibility-2026-04-25.md` |
| P3.12 | Long-horizon attention (persistent tasks) | open (needs pinned use case before code) | | |
| P3.13 | IntentionResolver Stage 1 | **deferred** (Stage 0 gates unmet 2026-04-24: backed=0/30, resolved=0/5, histogram=0/2) | | |
| P3.14 | Pi `/mind` consolidation (mental-world signals → particle visualizer) | **SHIPPED** 2026-04-25 (subtractive on Pi; minimal additive on brain feed; seven bounded scalars in `consciousness_feed.scene`; no entities / relations / vectors / authority flags) | code: 2026-04-25 ship | `p3_14_pi_mind_merge-2026-04-25.md` |
| P3.15 | Addressee inhibition gate for soft-signal multi-party context | **OBSERVATION** 2026-04-25 (one instance recorded; do not open lane until §9 conjunction recurs ≥3× in a 7-day window) | none — observation only | `emergent_addressee_overreach_observation-2026-04-25.md` |
| P3.16 | P5 scene-continuity aging/dedupe + display-contract cleanup | **SHIPPED** 2026-04-27 (bounded removed-entity retention; active/removed counts; active-canvas display contract; P5 stays PRE-MATURE / zero-authority) | code: 2026-04-27 ship | `p3_16_p5_scene_continuity_cleanup-2026-04-27.md` |

---

*This file is a contract with the next agent. Honor §1. Pick a lane in §3.
Run the workflow in §5. Write the evidence in `docs/validation_reports/`.
Commit locally. Cross the lane off in §7. Move on.*
